{{template "base" .}}
{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>This is the about page</h1>
                <p>This was taken from handlers.go: {{index .StringMap "test"}}</p>
            </div>
        </div>
    </div>
{{end}}